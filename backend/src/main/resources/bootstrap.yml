spring:
  application:
    name: zoey-admin-ui
  cloud:
    config:
      uri: https://zoey-stg.t-mobile.com/zoey-config
      username: zoey
      password:
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    restart:
      enabled: true
    health:
      show-details: always
    env:
      post:
        enabled: true